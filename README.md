# 🚀 TG Audio Downloader (RC1)

Удобный инструмент для автоматизированного скачивания аудиофайлов и обложек из форумов (топиков) Telegram-групп. Программа обладает интуитивным веб-интерфейсом, защитой от Flood-контроля и поддержкой многопоточной загрузки.

---

## 🛠 Быстрый старт

### 1. Подготовка системы
* **Python:** Требуется версия [Python 3.10+](https://www.python.org/downloads/).
* **Важно:** При установке на Windows обязательно поставь галочку **"Add Python to PATH"**.

### 2. Установка зависимостей
Открой терминал в папке проекта и выполни команду:
```bash
pip install -r requirements.txt

🔑 Настройка API и ID группы

    Ключи API: Получи свои api_id и api_hash на портале my.telegram.org.

    ID группы: Перешли любое сообщение из целевой группы боту @getmyid_bot. Скопируй ID из строки Forwarded from chat (он должен начинаться на -100).

    Файл настроек: Создай файл .env в корневой папке проекта на основе примера ниже:

API_ID=36345657
API_HASH=29f24977034r34r34af59caa5d00
PHONE=+79005553535
GROUP_ID=-1001215935287
HOST=localhost
PORT=8000

🚀 Запуск и использование

    Запусти сервер:

python main_new.py

Авторизация: При первом запуске введи в терминале свой номер телефона и код подтверждения, который придет в Telegram. Это создаст файл сессии.

    Открой интерфейс: Перейди в браузере по адресу http://localhost:8000.

Инструкция по работе:

    Нажми кнопку ОБЗОР и выбери папку на компьютере, куда будет сохраняться музыка.

    Отметь галочками нужные топики в списке и нажми СКАН ВЫБРАННЫХ.

    После появления статуса ГОТОВО, разверни топик (иконка папки), проверь список треков и нажми СКАЧАТЬ.

📁 Структура проекта

    main_new.py — основной серверный скрипт (Backend).

    static/index_new.html — пользовательский интерфейс (Frontend).

    .env — файл с твоими личными ключами и настройками (добавлен в .gitignore).

    requirements.txt — список необходимых библиотек для работы.

⚠️ Важные примечания

    Файлы сессии: Не удаляй файлы с расширением .session, иначе авторизацию придется проходить заново.

    Безопасность: Никогда не передавай свой файл .env третьим лицам.

    Flood Wait: Программа автоматически обрабатывает ограничения Telegram. Если сканирование или загрузка приостановились — просто подожди, процесс возобновится сам.
